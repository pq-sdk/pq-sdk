## PQSDK 

```
   Ed25519 , secp256k1 is already dead. Quantum computers will harvest every key ever exposed on-chain.
   PQSDK eliminates the attack surface entirely — no private key, no exposure, no quantum threat.
   Pure on-chain enforcement. No servers. No trust. 

```
